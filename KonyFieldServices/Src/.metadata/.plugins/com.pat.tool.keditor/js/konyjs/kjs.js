/*
 * A template file of additional java script functions 
 * 
 * @author kondal kolipaka
 */

/**
 * An alert box is often used if you want to make sure information comes through to the user.
 * syntax: alert("sometext");
 */
function alert(str) {}

/**
 * confirm box is often used if you want the user to verify or accept something.
 * syntax: confirm("sometext");
 */
function confirm(str) {}

/**
 * A prompt box is often used if you want the user to input a value before entering a page.
 * syntax: prompt("sometext","defaultvalue");
 */
function prompt(str) {}

